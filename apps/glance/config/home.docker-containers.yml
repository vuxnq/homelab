- type: docker-containers
  hide-header: true
  containers:
    
    caddy:
      name: caddy
      icon: di:caddy
      description: web server

    dufs:
      name: dufs
      icon: di:dufs
      url: https://dufs.tux.vuxnq.me
      same-tab: true
      description: file server

    glance:
      name: glance
      icon: sh:glance
      url: https://tux.vuxnq.me
      same-tab: true
      description: dashboard

    immich_server:
      name: immich
      icon: sh:immich
      url: https://immich.tux.vuxnq.me
      same-tab: true
      description: photo & video management
      id: immich
    immich_machine_learning:
      name: machine learning
      parent: immich
    immich_postgres:
      name: database
      parent: immich
    immich_redis:
      name: redis
      parent: immich

    syncthing:
      name: syncthing
      icon: sh:syncthing
      url: https://syncthing.tux.vuxnq.me
      same-tab: true
      description: continuous file synchronization
