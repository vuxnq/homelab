name: copyparty

services:
  copyparty:
    image: copyparty/ac:latest
    container_name: copyparty
    ports:
      - 3923:3923
    volumes:
      - ./copyparty.conf:/cfg/copyparty.conf:z
      - ./data:/cfg/copyparty:z
      - ./data/.hist:/.hist:z
      - /mnt:/w:z
    stop_grace_period: 15s
