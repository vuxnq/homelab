name: dufs

services:
  dufs:
    image: sigoden/dufs:latest
    container_name: dufs
    user: 1000:1000
    ports: 
      - 5000:5000
    volumes:
      - /mnt:/data
    command: /data -A
    restart: unless-stopped
